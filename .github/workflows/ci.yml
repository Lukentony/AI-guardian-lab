name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r guardian/requirements.txt
          pip install pytest requests-mock PyYAML anyio httpx pytest-mock litellm flask-limiter

      - name: Run test suite
        run: python -m pytest tests/ -v --tb=short

      - name: Docker smoke test
        run: |
          cp .env.example .env
          sed -i "s/API_KEY=CHANGE_ME/API_KEY=ci-test-key-123/" .env
          docker compose up -d --build guardian
          echo "Waiting for guardian to start..."
          sleep 20
          curl -f -H "X-API-Key: ci-test-key-123" http://localhost:5000/health
          docker compose down
