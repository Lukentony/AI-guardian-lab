version: "1.1-stable"
mode: "enforced"

zones:
  green:
    description: "Safe commands"
    binaries: [ls, cat, pwd, echo, date]
  
  yellow:
    description: "Logged commands"
    binaries: [curl, wget, git]
  
  red:
    description: "Dangerous commands"
    binaries: [rm, mv, chmod, sudo]
