service:
  name: "agent-mvp"
  version: "0.1.0"

endpoints:
  ollama: "http://<ollama-ip>:11434/api/generate"
  guardian: "http://guardian:5000"

timeouts:
  ollama: 30
  guardian: 5

execution:
  mode: "simulated"
